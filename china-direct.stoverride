name: 中国大陆直连
desc: |-
  抖音/字节大陆服务、中国大陆域名及 IP 优先 DIRECT，规则集每 24 小时更新。
  请在覆写列表中将本模块放在 Web3交易网络、AI工具使用上方，让本模块先应用。
  最终规则须为 Web3/AI 在前、大陆直连在后；其他流量交回原配置。

# Stash 官方覆写语法：rules 数组自动前插；不使用 prepend 或 #!replace。
# 覆写自上而下应用，后应用的模块规则会插到更前面：
# 列表从上到下：中国大陆直连 → AI工具使用 → Web3交易网络。
# 最终规则顺序：Web3 → AI → 中国大陆直连 → 原订阅规则。
# 不修改节点、策略组、DNS、UDP/QUIC、脚本或 MITM，也不添加 MATCH。
# 大陆规则集可能与特定服务重叠，必须保留上述 Web3/AI 优先级。
# 本模块用于规则模式；不能保证解决 DNS/CDN 或运营商引起的卡顿。

rule-providers:
  symonxu-china-direct-douyin:
    type: http
    behavior: classical
    format: yaml
    url: https://fastly.jsdelivr.net/gh/blackmatrix7/ios_rule_script@master/rule/Clash/DouYin/DouYin.yaml
    path: ./rule-providers/symonxu-china-direct-douyin.yaml
    interval: 86400
  symonxu-china-direct-domain:
    type: http
    behavior: domain
    format: yaml
    url: https://fastly.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/direct.txt
    path: ./rule-providers/symonxu-china-direct-domain.yaml
    interval: 86400
  symonxu-china-direct-ip:
    type: http
    behavior: ipcidr
    format: yaml
    url: https://fastly.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/cncidr.txt
    path: ./rule-providers/symonxu-china-direct-ip.yaml
    interval: 86400

rules:
  # 少量核心域名保证基本覆盖；CDN/API 的细节由维护中的抖音规则集补充。
  # 不使用 DOMAIN-KEYWORD,byte 或整个国际字节/TikTok 集团直连。
  - DOMAIN-SUFFIX,douyin.com,DIRECT
  - DOMAIN-SUFFIX,douyinvod.com,DIRECT
  - DOMAIN-SUFFIX,douyinpic.com,DIRECT
  - DOMAIN-SUFFIX,byteimg.com,DIRECT
  - DOMAIN-SUFFIX,snssdk.com,DIRECT
  - DOMAIN-SUFFIX,zijieapi.com,DIRECT
  - RULE-SET,symonxu-china-direct-douyin,DIRECT
  # direct.txt 是社区维护的大陆直连集合，也包含部分境外服务的大陆可直连域名。
  - RULE-SET,symonxu-china-direct-domain,DIRECT
  # 不加 no-resolve：域名集合未覆盖时，也需要解析并匹配大陆目标 IP。
  - RULE-SET,symonxu-china-direct-ip,DIRECT
  # 补充客户端 GeoIP 数据库的 CN 覆盖（含其数据库支持的 IPv6）。
  - GEOIP,CN,DIRECT
